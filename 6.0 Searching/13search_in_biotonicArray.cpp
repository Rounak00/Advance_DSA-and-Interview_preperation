//we should have two binary search function first and find peak element function also

//now after find peak element 

// ascbs(0,mid-1,arr,target) and dscbs(mid,n-1,arr,target) o think one of them obviously find ans and other 1 find -1 so return where we get ans